feature_id: F-099
module_id: M-004
slug: quan-ly-dai-inmarsat-cap-nhat

# Map logical feature → physical code locations.
# Primary owner = service that implements feature's main flow.
# Consumers = services/apps that read or invoke this feature.
implementations:
  primary: "src/main/java/com/hanghai/kchtg/station/controller/CoastalStationInmarsatController.java -> updateStation()"
  consumers:
    - "src/main/java/com/hanghai/kchtg/station/service/CoastalStationInmarsatService.java"

# Cross-cutting: features consumed by multiple modules
consumed_by_modules: []
