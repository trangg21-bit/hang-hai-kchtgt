feature_id: F-074
module_id: M-004
slug: quan-ly-phao-tieu-tao-moi

# Map logical feature → physical code locations.
# Primary owner = service that implements feature's main flow.
# Consumers = services/apps that read or invoke this feature.
implementations:
  primary: "src/main/java/com/hanghai/kchtg/beacon/service/BuoyService.java#create"
  consumers:
    - "src/main/java/com/hanghai/kchtg/beacon/controller/BuoyController.java#create"

# Cross-cutting: features consumed by multiple modules
consumed_by_modules: []
