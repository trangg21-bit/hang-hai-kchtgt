<!-- Machine-generated from docs/intel/_intake/TRI-1787735605077-dc09.json; edit the triage record through intake, then re-scaffold. -->
# C2 Execution Brief

Canonical triage record: `docs/intel/_intake/TRI-1787735605077-dc09.json`

## What

Fix toàn bộ lỗi compile backend do refactor xóa 3 bảng (approval_logs/change_logs/station_history, migration V20260825162500) để sót code chết: (1) khối legacy ApprovalLog/ChangeLog bị merge chồng ở DryPort/Pier/Berth ApprovalService; (2) tham chiếu StationHistory/StationHistoryRepository đã xóa trong BuoyStationService; (3) dead block if(changeLogRepository/approvalLogRepository != null) với field không tồn tại trong ChangeHistoryService/ApprovalWorkflowService.

## Entry Point

API phê duyệt KCHT cảng + CRUD nhà trạm phao: /api/v1/dry-ports/.../approve, PierController:152, BerthController:149, BuoyStationService CRUD, CommonOptionsService

## Oracle

mvn compile exit 0 (BUILD SUCCESS) toàn backend — không còn lỗi biên dịch; controller gọi đúng signature approve/reject; luồng phê duyệt 2 cấp + lịch sử infrastructure_history không đổi.

## Evidence

Use the canonical triage record's verified `seam_claims` and `evidence.edit_target_files`; do not re-discover the whole project unless one of those anchors is stale or missing.
