feature_id: F-105
module_id: M-004
slug: quan-ly-dai-cospas-sarsat-cap-nhat

# Map logical feature → physical code locations.
# Primary owner = service that implements feature's main flow.
# Consumers = services/apps that read or invoke this feature.
implementations:
  primary: "src/main/java/com/hanghai/kchtg/station/controller/CoastalStationCospasSarsatController.java -> updateStation()"
  consumers:
    - "src/main/java/com/hanghai/kchtg/station/service/CoastalStationCospasSarsatService.java"

# Cross-cutting: features consumed by multiple modules
consumed_by_modules: []
