import PortTerminalAssetList from './PortTerminalAssetList';
import { DIKE_REVETMENT_ASSET_SCREEN } from './infrastructureAssetScreen';

export default function DikeRevetmentAssetList() {
  return <PortTerminalAssetList screenConfig={DIKE_REVETMENT_ASSET_SCREEN} />;
}
